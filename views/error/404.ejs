<%- include('../partials/head', { title: title || 'Error' }) %>
<%- include('../partials/ambient') %>
<div class="auth-wrap">
  <div class="auth-card">
    <div class="auth-logo"><span style="font-size:52px;font-weight:800;color:var(--accent)"><%= code || 'Error' %></span></div>
    <h1 class="auth-title"><%= title || 'Something went wrong' %></h1>
    <p class="auth-sub"><%= message || '' %></p>
    <a href="/" class="btn btn-primary btn-block">Back to home</a>
  </div>
</div>
<%- include('../partials/foot') %>
